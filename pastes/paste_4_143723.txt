    | GhidraMCP 连不上 | WSL2 的 127.0.0.1 无法转发到 Windows 的 8080 | 改用 Windows 宿主机 IP 172.19.240.1 |
    | HTTP 服务没启动 | 扩展勾选后没重启 Ghidra | 已解决 |

    ⚠️ 注意： 172.19.240.1 是 WSL2 动态分配的，重启 WSL 后可能会变。如果想一劳永逸，可以：

    - 方案 A： Windows 防火墙添加入站规则允许 8080 端口，然后用 localhost 就能通
    - 方案 B： 写个脚本启动时自动解析 Windows IP

    先重启 Hermes 试试当前配置能不能用吧。/new 就行。